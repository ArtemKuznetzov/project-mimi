services:
  auth-service-db:
    ports:
      - "5001:5432"

  chat-service-db:
    ports:
      - "5002:5432"

  minio:
    volumes:
      - ./minio-data:/data

networks:
  internal:
    internal: false